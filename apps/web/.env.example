# Apps/web — variáveis públicas (prefixadas NEXT_PUBLIC_, vão para o bundle)

# URL base da API NestJS (host público; porta interna 13001 atrás de proxy)
NEXT_PUBLIC_API_BASE_URL=https://api-uniats.unifique.com.br

# Azure AD — se vazio, frontend roda em "modo dev" sem login real.
NEXT_PUBLIC_AZURE_AD_TENANT_ID=
NEXT_PUBLIC_AZURE_AD_CLIENT_ID=
# Escopo que o token precisa cobrir para chamar a API (ex.: api://uniats-api/access)
NEXT_PUBLIC_AZURE_AD_API_SCOPE=
